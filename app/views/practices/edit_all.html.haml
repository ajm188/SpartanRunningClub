%h1.page-header Edit Practice Schedule

-if @practices.empty?
	You have no practices!
-else
	%table.table.table-striped
		%tbody
			-@practices.each do |practice|
				%tr
					%td=practice.day
					%td=practice.time_string
					%td
						=link_to 'Delete', practice, method: 'delete', class: 'btn btn-xs btn-danger', confirm: 'Are you sure?'
.form-actions
	=link_to 'Add practice', new_practice_path, class: 'btn btn-primary'
	=link_to 'Back', admin_panel_path, class: 'btn btn-default'
