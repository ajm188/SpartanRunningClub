=form_for @practice, html: { class: 'form-horizontal' } do |f|
	-if @practice.errors.any?
		#error-explanation
			%h4
				=pluralize @pratice.errors.count, 'error'
				prohibited this practice from being saved
			%ul
				-@practice.errors.full_messages.each do |msg|
					%li=msg
	.control-group
		=f.label :day, 'Day', class: 'control-label'
		.controls
			=f.select :day, Practice::DAYS, {}
	.control-group
		=f.label :time, 'Time', class: 'control-label'
		.controls
			=f.select :hour, Practice::HOURS, {}, class: 'span1'
			\:
			=f.select :minute, Practice::MINUTES, {}, class: 'span1'
			=f.select :am_pm, Practice::AMPM, {}, class: 'span1'
	.form-actions
		=f.submit 'Save', class: 'btn btn-primary'
		=link_to t('.cancel', default: t('helpers.links.cancel')), practice_edit_path, class: 'btn'
