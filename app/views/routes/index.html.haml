-header_tag do
	%h1 Routes

-if @routes.any?
	-table_tag do
		%thead
			%tr
				%th Title
				%th Distance
		%tbody
			-@routes.each do |route|
				%tr
					%td=link_to route.title, route
					%td=route.distance
